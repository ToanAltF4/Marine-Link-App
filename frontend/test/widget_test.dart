// Placeholder widget test — will be replaced with proper tests in Sprint 1.
// Default test removed to avoid import errors from generated starter code.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — widget tests will be added in Sprint 1', () {
    expect(true, isTrue);
  });
}
